const verifyToken = require("./verifyToken");
const getData = require("./getData");
const permit = require("./permit");
const password = require("./password");

module.exports = { verifyToken, getData, permit, password };
